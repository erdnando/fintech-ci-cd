node {

    checkout scm

    env.DOCKER_API_VERSION="1.23"
    
    sh "git rev-parse --short HEAD > commit-id"

    tag = "latest"   
    appName = "front-fintech"
    registryHost = ""
    imageName = "${registryHost}${appName}:${tag}"
    env.BUILDIMG=imageName
    
    stage "Build"
    
        sh "docker build -t mx.com.smi.services:30402/front-fintech:latest -f aplicaciones/01vuejs/Dockerfile aplicaciones/01vuejs"
    
    stage "Push"

         withDockerRegistry([ credentialsId: "docker-private-credentials", url: "http://mx.com.smi.services:30402" ]) {
            sh "docker push mx.com.smi.services:30402/front-fintech:latest"
          }
}
