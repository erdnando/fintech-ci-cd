node {

    checkout scm

    env.DOCKER_API_VERSION="1.23"
    
    sh "git rev-parse --short HEAD > commit-id"

    tag = "latest"   
    appName = "front-fintech"
    registryHost = ""
    imageName = "${registryHost}${appName}:${tag}"
    env.BUILDIMG=imageName
    
    stage "delete"
    
        sh "kubectl -n smi delete -f https://github.com/erdnando/fintech-ci-cd/blob/master/yamls/front-a-deployment.yaml"
    
    stage "apply"
    
        sh "kubectl -n smi apply -f https://github.com/erdnando/fintech-ci-cd/blob/master/yamls/front-a-deployment.yaml"
}
